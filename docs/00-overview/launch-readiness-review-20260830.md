# 上线审查：九天里补的全是「说清楚」，能上线的东西一件都没多 —— 节点仍是 0 台，商业链仍是 0

> 日期：2026-08-30 · 性质：**证据型核查** · 状态：**As-Built**（2026-08-30 时点快照，不随后续进展回改）
> 事实基线：master `a4604c9396f`（2026-08-30）；**0 个 open PR**；
> ⚠️ **审查开始时工作树干净，收尾时不干净了** —— 同一天另有一条并行工作流在 `api/` 下改动
> （管理面/内部面鉴权接线 + 5 个新测试文件，**未提交**）。
> **本文全部数字以 HEAD `a4604c9396f` 的已提交内容为准**（用 `git ls-tree -r HEAD` / `git show HEAD:<file>` 取数，
> 不是 `find` 工作树），未提交的改动只在 §7 的 B48 行里单独说明，不计入任何统计；
> 本文每一个数字都由本次审查当场跑命令得到，命令写在正文或脚注里，**未做任何推算，未沿用任何旧数字**
> 关联：[roadmap.md](roadmap.md)（B1–B49 阻塞项登记处，同日复核）、
> [launch-readiness-review-20260821.md](launch-readiness-review-20260821.md)（**As-Built，不回改**；§9 是对它的逐条对读）、
> [product-brief.md](product-brief.md)、[pricing-and-plans.md](../03-product/pricing-and-plans.md)、
> [docs/README.md §7](../README.md)
> 证据口径：本机命令实测 / GitHub API 实况 = **高**；仓库文档转述 = 中；未复核的旧结论 = **不采用**
> 读者：决定「离上线还差什么」的人。审查目标：**上线并服务内部付费用户**。
> ⚠️ **本文与 [20260821 那份](launch-readiness-review-20260821.md) 并列存在，不替代它。**
> 那份是 2026-08-21 的时点快照且明写不回改；本文是 2026-08-30 的。两份对读见 §9。

---

## 1 · 结论

**九天前的判定是「P1 内核过半、商业链为零」。九天后，这句话一个字都不用改。**

变的是**文档与判断的质量**，不是**可上线的东西**：

| 这九天真正发生的 | 这九天没有发生的 |
|---|---|
| 六份 ADR（0010–0015）落库，把域名策略、失联检测、支付、计费退款、SLO、客户端六个洞各写成一份带正反辩论的裁决 | **六份全是「提案，未批准」**，一份都没生效 |
| 定价从「价格全部留空」变成三档 ¥72 / ¥159 / ¥358 定案，带 24 格覆盖表与 11 条失效条件 | **一分钱都还没收过**；定价的第二个前置 `nettier-ab-*` 仍未做，所以只到「设计稿 v1」 |
| 前端从「测试一行都没有」变成 **108 个用例 / 7 个文件全绿**，登录态与 16 条受保护路由落地 | 关键路径 8 页只接了 2 页；**后台 17 个模块一页都接不了**（鉴权没接线） |
| 迁移从 12 组涨到 **17 组 / 44 张表**，支付与计费的 schema 全部就位 | 表在库 ≠ 有读写路径；而且它们是按**未获批准**的 ADR 0012/0013 建的形状 |
| CI 从 8 个作业涨到 **9 个**，新增两步真打 Postgres 的检查（EXPLAIN 194 条 SQL + 回滚后写探针） | **`deploy.yml` 仍然一次都没运行过**，35 次 workflow run 全是 `ci` |

**三个最紧迫的发现，都不是九天前那三个：**

1. 🔴 **「域名一个都没注册」这个被反复引用的前提，是错的。**
   `dig +short NS babel.plus` → `dns13.hichina.com.` / `dns14.hichina.com.`（2026-08-30 实查）。
   `babel.plus` 已注册、DNS 可控、生产 `BP_ALLOWED_ORIGINS` 一直指着它。
   而 [20260821 那份](launch-readiness-review-20260821.md) §1/§2.2/§3 三处、
   [ADR 0014](../05-adr/0014-slo-and-oncall.md) 三处、roadmap §4.4 的「P1 硬前置」第一条，
   全部建立在「域名 0 个注册」上。
   **真正缺的是 ADR 0010 那 5 个中性镜像主域名（未采购），那是另一件事。**
   直接后果：**P1 的 Hysteria2 / DNS-01 证书链今天就能开始搭，它并没有被卡住。**
2. 🔴 **一次「schema 改形状」在生产的付款链上炸过，而 CI 对整类问题全盲。**
   迁移 `0016` 把 `users.transfer_enable` 变成 GENERATED STORED 之后仍有 8 条查询在写它；
   `api/sqlc.yaml` **没有 `database:` 段**，所以 `sqlc generate` 与 `go build` **都 exit 0**，
   **第一次暴露是在用户付款成功、订单进 paid、开通权利那一刻返 500**。
   已修，并补了两步真打 Postgres 的 CI 检查（新登记为 roadmap **B49**）。
   ⚠️ **根因没消除**：`sqlc.yaml` 依旧没有 `database:` 段，我们是在下游加了两道网。
3. 🔴 **`deploy.yml` 从未运行过，仓库里 0 个 environment / 0 个 variable / 0 个 secret。**
   写下来的部署路径与实际用的部署路径是两条不同的东西（新登记为 roadmap **B47**）。
   九天前的头号发现是「生产与 master 漂移」—— 那条**已经消除**（PR 全部合并，0 个 open PR），
   但它暴露的根问题「**没有一条被 CI 验证过的部署路径**」原封不动。

---

## 2 · 现状盘点（当前实现，非设计目标）

### 2.1 代码 —— 每个数字都附产生它的命令

| 层 | 数字 | 命令（本次当场跑过） |
|---|---|---|
| OpenAPI 契约 | **7,065 行 / 128 个 operation** | `wc -l openapi/openapi.yaml`；operation 数取 `wc -l api/internal/handler/operations.txt` |
| API 已实现 | **18 / 128** | `grep -hoE "^func \(s \*?Server\) [A-Za-z]+" api/internal/handler/*.go \| awk '{print $4}' \| sort -u` 与 `operations.txt` 取交集 |
| API 仍返 501 | **110** | 同上取差集（128 − 18） |
| 迁移 | **17 组 up/down**（`0001`–`0017`） | `ls api/db/migrations/*.up.sql \| wc -l` |
| 表 | **44 张** | `grep -rhoiE 'CREATE TABLE (IF NOT EXISTS )?[a-z_."]+' api/db/migrations/*.up.sql \| sort -u \| wc -l` |
| sqlc 查询 | **194 条** | `grep -rhoE "^-- name: [A-Za-z]+" api/db/queries/*.sql \| wc -l`（与 `db/gen/*.sql.go` 的 SQL 常量数一致） |
| Go 测试 | **20 个 `_test.go` / 195 个顶层 `Test` 函数**（HEAD 口径） | `git ls-tree -r --name-only HEAD \| grep "_test.go$" \| wc -l`；对同一批文件跑 `git show HEAD:<f> \| grep -cE "^func Test"`。⚠️ **工作树里是 25 / 275**，多出来的是同日未提交的鉴权接线测试，**不计入本文统计**（见 §7 B48） |
| Go 源码 | 非生成 **19,513 行** / 生成物 **30,775 行** | `find api -name "*.go" \| grep -v "/gen/" \| xargs wc -l`（与其反向） |
| web 测试 | **108 个用例 / 7 个文件，全绿** | `pnpm -r test`（shared **67** + user **33** + admin **8**） |
| web 路由组件 | user **24** + admin **23** = **47** | `find web/{user,admin}/src/routes -name "*.tsx" \| grep -v test \| wc -l` |
| web 未接线点 | **44 处 `TODO(P1)`，散在 30 个文件** | `grep -rho "TODO(P1)" web --include="*.ts*" \| wc -l` |
| infra 脚本 | **18 支 / 10,705 行** | `find infra -name "*.sh" \| wc -l`；`find infra -name "*.sh" -exec cat {} + \| wc -l` |
| CI 作业 | **9 个** | 从 `.github/workflows/ci.yml` 的 `jobs:` 下解析：`changes` `go` `contract-drift` `migrations` `openapi-lint` `web` `shellcheck` `docker-build` `ci-ok` |
| evidence 目录 | **9 个，9/9 都有 README** | `ls -d docs/evidence/*/ \| wc -l` |
| ADR | **14 份**（`0001`–`0008` + `0010`–`0015`，`0009` 是刻意空号） | `ls docs/05-adr/0*.md \| wc -l` |
| docs 文档 | **56 篇 `.md`**（含本文） | `find docs -name "*.md" \| wc -l` |
| open PR | **0 个**（历史共 18 个：17 merged + 1 closed） | `gh pr list --state open`；`gh pr list --state all --limit 50` |
| workflow run | **35 次，100% 是 `ci`** | `gh run list --limit 200 --json workflowName` → `Counter({'ci': 35})` |
| **deploy 运行次数** | 🔴 **0** | `gh run list --workflow=deploy.yml --limit 50` → 空 |
| repo environment / variable / secret | 🔴 **0 / 0 / 0** | `gh api repos/oratis/babelplus/environments`、`.../actions/variables`、`.../actions/secrets` → `total_count` 全为 0 |
| api 代码内 TODO | P1 **5 条** / P2 **27 条** | `grep -rho "TODO(P[0-9])" api --include="*.go" --include="*.sql" \| sort \| uniq -c` |

> ⚠️ **一处必须说明的方法缺陷：本机没有 Go 工具链**（`which go` 无结果），
> 所以 **195 这个数字是静态计数，不是一次 `go test ./...` 的绿灯**。
> 本文不声称 Go 测试全绿；只声称仓库里有 195 个顶层 `Test` 函数。
> web 侧的 108 是**真跑出来的**（`pnpm -r test` 全绿）。

### 2.2 GCP 实况

**未在本次审查中重新执行 `gcloud`**（本次是仓库侧审查）。
以 [as-built-gcp §10](../02-architecture/as-built-gcp.md) 的 2026-08-20 复核为准，逐条转述并标明口径：

已建成并运行：`bp-api`（Cloud Run，`us-central1`，2026-08-17 创建，`maxScale=8`，
`BP_DB_MAX_CONNS=2`，startup CPU boost 开，敏感值全走 Secret Manager `secretKeyRef`）、
`bp-db`（PostgreSQL 17 / `db-f1-micro`，稳态 $9.53/月）、3 个服务账号、4 个 Secret、
Artifact Registry `bp-images`、2 条 Cloud Tasks 队列、Pub/Sub `bp-alerts`。

🔴 **不存在**：任何 `bp-node-*` 节点（**0 台**）、`bp-web` 部署、任何 Cloud Scheduler 作业、
17 条告警策略中的任何一条、任何 uptime check。

---

## 3 · 阶段完成度 —— 两个数字，因为它们差得很远

**roadmap 自己的口径是「出口标准」**（§1 组织原则 2：「每个阶段的出口标准必须是一次可判定的观察，
不是一份文档」）。按那个口径算一次；再按「工作量」算一次；**两个都写，并说清楚差在哪**。

| 阶段 | **按出口标准（roadmap 口径）** | 按工作量 | 差在哪 |
|---|---|---|---|
| **P0 调研与设计** | **3.5 / 7 = 50%** | ≈ 90% | 见 §3.1 |
| **P1 内核可用** | 🔴 **0 / 8 = 0%** | ≈ 45% | 见 §3.2 —— **八条出口标准全部要求一台在跑的节点，而节点数是 0** |
| **P2 产品闭环** | **0 / 8 = 0%** | ≈ 15% | 见 §3.3 |
| **P3 可运营** | **0 / 7 = 0%** | ≈ 5% | 脚本写完了，一条告警都没建，演练零次 |
| **P4 加固** | **0 / 5 = 0%** | ≈ 5% | 只有「生成物漂移」这一小块做了，IaC 未起步 |

> **为什么两列差这么多，一句话：出口标准全是「被观察到」，而我们这九天做的全是「被写下来」。**
> 这不是批评，是这套口径的设计目的 —— roadmap §1 原则 2 就是为了让这个差值可见。

### 3.1 P0 逐条（3.5 / 7）

| # | 判据 | 判定 | 依据 |
|---|---|---|---|
| 1 | ADR 0001 不再是「提案，未批准」 | ✅ | `0001-cloudflare-tos-risk.md:3` = 「已批准，待实施（2026-08-17 用户批准）」 |
| 2 | evidence ≥ 5 个目录，每个带 README 且写明「证明什么 / 不证明什么」 | ✅ **9 个，9/9 有 README** | 最后一个缺 README 的 `ipv6-censorship-20260817/` 已于 2026-08-29 补上（本次复核了它的 `sha256` 与逐字段解包，与 JSON 原文一致） |
| 3 | pricing 里不再有留空的价格；§7 第一条被划掉 | ✅ | `grep -n "待定" docs/03-product/pricing-and-plans.md` **无命中**；§7 第一条已是 `- [x]` |
| 4 | docs/README §7 阻塞项表第 1、2 条被划掉 | 🔶 **0.5** | 第 1 条已划（ADR 0001）；**第 2 条「零实测数据」原封不动** |
| 5 | v2node 三项行为各有一条 evidence | 🔶 **0** | 三项**答案**都有（读源码），但落在 `v2node-contract-20260817` 与 `v2node-401-behavior-20260821`，**没有 `evidence/v2node-behavior-*`**；且判据要的「起容器」一次没做 |
| 6 | 三份缺失 ADR 各自存在，**且不是「提案，未批准」** | 🔴 **0 / 3** | 0010 与 0011 **存在但状态正是被排除的那个**；**节点密钥传输形式那一份连文件都不存在**（`ls docs/05-adr/` 14 份里没有） |
| 7 | 至少一个域名已注册且 DNS 可控 | ✅ **而且一直是** | `dig +short NS babel.plus` → `dns13/dns14.hichina.com`；`dig +short SOA babel.plus` → `… 2026082617 3600 1200 86400 600` |

**按工作量看 P0 却接近 90%**：38 篇设计文档、14 份 ADR、9 个证据目录、定价定案。
**差值几乎全部落在标准 6 上** —— 六份新 ADR 写得又长又细（每份都带正反辩论与逐条裁决），
但**一份都没被批准**，而标准 6 把「未批准」明确排除在通过之外。

> 🔴 **这是本次审查最想说清楚的一件事：`ADR 数量` 是工作量指标，`ADR 状态` 才是完成度指标。**
> 九天里 ADR 从 8 份涨到 14 份，P0 的出口标准 6 却从 0/3 变成 0/3。

### 3.2 P1 逐条（0 / 8）—— 八条全部要求一台在跑的节点

| # | 判据（roadmap §4.3 原文缩写） | 判定 | 为什么 |
|---|---|---|---|
| 1 | `bp-node-hk1` 通过 J1–J6，含 ≥1 次晚高峰采样 | 🔴 | **没有 `bp-node-hk1`** |
| 2 | v2node 从 `bp-api` 拉到配置与用户表，180 秒窗口 `1×200 + 2×304` | 🔴 | 没有节点，也没有起过 v2node 容器 |
| 3 | 若第 2 条不成立，已切降级方案并把损失写进 evidence | 🔴 | 前提未发生 |
| 4 | 一条真实订阅链接在 Clash Verge Rev 与 sing-box **各人工加载一次成功** | 🔴 | 没有节点就没有真实订阅链接。而 B45（sing-box 缺 `inbounds`）与 B46（`GEOIP,CN` 拒载）都指向这一步会出事 |
| 5 | 连续 72 小时无中断；内存峰值 < 70%；流量差异 < 1% | 🔴 | 没有节点 |
| 6 | 封禁 / 配额耗尽 / 到期三态各手工触发一次并计时 | 🔴 | 没有节点；**且这三态依赖 9 个 `RunXxxTask` 端点，它们全部 501**（B48） |
| 7 | 一次节点密钥两步轮换，节点全程不失联 | 🔴 | 没有节点 |
| 8 | 每阶段前后跑 as-built §7 清点做 diff，`vpn-*` 与三个 Cloud Run 服务零变化 | 🔴 | 没有「阶段」可跑 |

**按工作量看 P1 却有 ≈45%**：API 侧 18 个 operation 上云、44 张表在库、
订阅下发与 UniProxy 四个端点实现、节点脚本 10,705 行写完。
**差值 100% 落在「节点数 = 0」这一件事上** —— 八条判据没有一条能在没有节点时被观察到。

> **这就是「按出口标准算 0%、按工作量算 45%」的全部含义**：
> P1 的产出不是「造出了半个节点」，是「造好了建节点需要的一切，但一次都没执行」。
> `infra/node/` 下 2,621 行脚本全部带 dry-run。**写了脚本 ≠ 执行过。**

**API 侧仍欠的（不需要节点就能做）**：
`/status` 端点未实现（五端点实现了四个）；ESP 发信未接通；
六条 Cloud Scheduler 一条没建；11 条 log-based metric 建了 7 条；
**管理面与内部面鉴权未接线**（中间件已写在 `api/internal/middleware/{admin,internal}.go`，
但 HEAD 的 `authmap.go` 还没接上，也没有对应测试 —— B48；
⚠️ **同日未提交的工作树里已经接上了，见 §7 的 B48 行**，
但**接线 ≠ 端点可用**，61 个 admin handler 绝大多数仍是 `Unimplemented` 的 501）。

### 3.3 P2 / P3 / P4（各 0%）

- **P2 出口 8 条全 0**：第 2 条「≥1 笔真实链上收款完成对账」是硬判据，而支付 operation 全部 501。
  唯一有实质进展的是第 8 条的前置（「定价页每个数字可追溯到 evidence」）——
  定价推导已写成 `pricing §3.5`，但**定价页本身不存在**。
  🔴 **邮件是这一层最安静的洞**：ESP 未选、**发信一行没接通**，
  而 ADR 0002 裁决邮件是**唯一**的失联恢复通道 ——
  **今天发生一次域名封锁，我们没有任何一条能通知到用户的路径。**
- **P3 出口 7 条全 0**：17 条告警策略一条没建，演练零次。
  `setup-alerts.sh` 1,191 行 + `setup-metrics.sh` 951 行已写好并过 shellcheck，**从未执行**。
- **P4 出口 5 条全 0**：唯一做掉的是「`git diff --exit-code` 卡 OpenAPI 漂移」
  （CI `contract-drift` 作业），而那只是第 2 条判据的三分之一。
  **真实 Postgres 的集成测试与真实 v2node 容器的契约测试都不存在** ——
  195 个 Go 测试**全部是进程内单元测试**（假 `Querier` / `httptest`）。

---

## 4 · 单位经济 —— 九天前算不出来，现在算得出来了

九天前这一节的第一句是「这笔账目前算不出来」。**现在算得出来，这是本期最实质的一处进展。**

| 数字 | 值 | 出处 |
|---|---|---|
| 定价基准单价 `u` | **$0.11/GiB**（Standard 目录价） | Billing Catalog API；**且它是本项目全生命周期唯一适用的档位**（下一档需 10,240 GiB/源区域/月 ≈ 95 名付费用户，超出 product-brief §4「规模有上限」）—— 缓冲是 0，「分档上界方向安全」已撤回 |
| 有效变动单价 `c = u × k` | **$0.121/GiB** | `k = 1.10` 🔴 **设定值，无实测依据** |
| 三档定价 | **¥72 / ¥159 / ¥358** | 30 / 100 / 250 GiB，2 / 5 / 10 设备（[pricing §3.1](../03-product/pricing-and-plans.md)） |
| 24 格覆盖倍数 | **1.2749× ~ 1.5616×** | 最薄格 = 重度·年付·USDT；含一次性返佣后 **1.2620×**，全 24 格无一破 1.20× 地板 |
| 满编成本上界 | **$412.24/月** gross | 8/12/5 拆分、2,690 GiB；落在线上 $500 预算的 **82.4%** |
| 满编收入 | **¥4,274 = $597.76/月** | 覆盖 **1.4500×** —— 这是**下界**（满额兑付 + 免费额度记 0） |
| 竞品零售 | **$0.04505/GiB** | **仅为 GCP Standard 全球最便宜源区域目录价（Oregon $0.085）的 53.0%** —— 即便零固定成本零毛利也卖不到这个价。**不是效率差距，是商业模式差距** |
| 本项目本窗口净现金 | 约 **$6** | gross $332.91 全部被账单账户级推广抵扣吸收 |

🔴 **两个仍然悬空的参数，它们决定这张表值多少钱**：
`k = 1.10`（**无出处、无实测，直接乘在变动成本上**，要第一台节点跑满一个账期才能对账）与
`FX = 7.15`（**二手反推**，且 2026-08-23 的检索聚合显示现汇可能是 6.72–6.74，
**我们可能在超收 6.1%** —— 在「成本分摊而非利润最大化」的定位下，多收 6% 不是安全边际，是超收）。
**汇率核实是一次查询就能关掉的事，已提为上线硬前置。**

---

## 5 · 这九天修掉的文档矛盾（因为它们本身就是一种技术债）

| 矛盾 | 修法 |
|---|---|
| `README.md` 说「122 个返回 501」 | 真值 **110**（18 已实现）。旧数是 2026-08-17 只实现 6 个时的 |
| `README.md` 说「db/migrations/ 12 组」 | 真值 **17 组** |
| ADR 0008：`README.md` 说「至今未实施」，ADR 头部说「已实施（仅新节点）」 | **两种读法各对一半，改写成一句同时容纳两半的话**：新节点已硬编码 Standard 且建后断言，既有 `vpn-*` 仍 Premium 且裁决明定不迁，**所以在花钱的那部分一分钱都没省** |
| `docs/05-adr/README.md` 同一文件 15 行内自相矛盾（babel.plus 归属已答 vs 仍待答且卡生产 CORS） | 订正为一件待办：归属 2026-08-25 已答，**而答案落在情形 (a) 上的处置恰恰是「什么都不要动」**；剩下的唯一一件是域名采购下单 |
| ADR 0005 有**三个**状态（提案未批准 / 设计稿 v1 / 设计稿 v1 待实施） | 统一为 **执行中**，并**显式保留「未批准」这一半** —— `bp-db` 是**先建后批**的 |
| ADR 0006 §15 同一节里「未验证」与「✅ 已解决：发」并列 | 把被推翻的那条划掉并交代：**追加式更新保住了历史，却在同一节留下两个相反的现值** |
| `CONTRIBUTING.md` 说「当前只有 httpx 的单测」 | 真值 **20 个测试文件 / 195 个 Test 函数**；⚠️ **相邻半句仍为真且保留**（无集成测试、无真 Postgres、无真 v2node 容器） |
| `web/README.md` H1 说「全部是空壳 / 业务逻辑为零」，而它自己 §8–§9 记着登录态、16 条守卫路由、108 个测试 | 改写 H1 与头部；**同时把「绝大多数仍是空壳」说得更硬**（44 处 `TODO(P1)`、47 个路由组件只有 3 个调过 API） |
| `data-model.md` / `api-contract.md` 状态仍写「未实施」 | 改 **执行中**，并各自写明「表在库 ≠ 表在用」「契约冻结 ≠ 端点可用」 |
| `monitoring.md` 头部说「一条策略都没建过」，§3.2 说 2026-08-21 建了 7 条 | 改 **执行中**，逐条列出 7 建 4 未建；**并保留「待实施」的那一半**（17 条告警一条没建、演练零次） |
| `monitoring.md` §9 说 budget 告警「现在建不了」 | ✅ B32 已解，**且原诊断是错的** —— 不是没权限，是**口径**（`INCLUDE_ALL_CREDITS` 被抵扣冲平，永远不触发）。roadmap R6 与 §12 代价 5 同步订正 |
| `monitoring.md` §6.2/§7、`deploy.md` 健康检查写 `"ok":true` | 实现返回**三个字节 `ok`**。照原文建的 check **会从第一天永久报红** —— 顺带发现 deploy.md 还有三处偏离（路径应为 `/-/healthz`、无 `revision` 字段、**`/healthz` 实际会查 DB 而本节明令不许**） |
| 定价修订说明未被执行（PR #18 只落库指令） | **本次执行完毕**：三档填 ¥72/¥159/¥358，定价推导写成 `### 3.5`，**§6/§7 编号一个字不动** |

> 🔴 **为什么「§6/§7 不重编号」值得单独说**：修订说明 §1.3 用 `grep` 实测证明重编号会打断
> **15 处、跨 8 个文件**的引用，其中 [roadmap §3.3](roadmap.md) 出口标准第 3 条是一条
> **本已满足**的验收标准（「§7 第一条被划掉」）——
> 重编号会让它指向永远不会被划掉的「代价」第一条，
> **一条本已满足的验收标准会静默变成永远无法满足。**

---

## 6 · 需用户拍板的决策

九天前列了 9 条。**现在其中 6 条有了裁决草案，但草案不是批准 —— 所以 9 条一条都没关掉。**

| # | 决策 | 九天后的变化 |
|---|---|---|
| 1 | **域名策略** | 🔶 [ADR 0010](../05-adr/0010-domain-strategy.md)（**提案，未批准**）。✅ 附带订正：`babel.plus` 已注册且 DNS 可控，**「零域名」不再卡 P1**；缺的是 5 个镜像域名（**采购不可退，需用户本人付钱**） |
| 2 | **退款政策** | 🔶 [ADR 0013](../05-adr/0013-billing-and-refund-rules.md)（**提案，未批准**）。数据库侧已有 `refunds_cooling_off_once` 约束 —— **即数据库已按未获批准的裁决改了形状** |
| 3 | **支付网关 + AML** | 🔶 [ADR 0012](../05-adr/0012-payment-gateway.md)（**提案，未批准**）：不部署 EPUSDT、不接易支付、自扫链、不归集。**AML 方案仍完全未定** |
| 4 | **`vpn-us` / `vpn-jp` 上是否有人在用** | 原封不动。**且它现在同时卡定价** —— [pricing §3.5.3](../03-product/pricing-and-plans.md) 的配额论证是**总量对总量**，若使用者只有 1–2 人，人均结论完全反转 |
| 5 | **升级折抵 + 流量包重置** | 🔶 [ADR 0013](../05-adr/0013-billing-and-refund-rules.md)；`transfer_enable` 已在迁移 `0016` 拆成 `_plan` + `_pack`。**降档 / 多次升档 / 加油包余量三种情形仍无算式** |
| 6 | **是否采购境内探测能力** | 原封不动。它卡着 `nettier-ab-*`，而那是**定价定稿的第二个前置** |
| 7 | **iOS 首推客户端** | 🔶 [ADR 0015](../05-adr/0015-client-strategy.md)（**提案，未批准**） |
| 8 | **设备数限制的产品口径** | 🔶 [ADR 0015](../05-adr/0015-client-strategy.md)。**且它现在是定价失效条件之一**：`/alivelist` 失败时 v2node 静默当作「0 台在线设备」，阶梯一塌，「轻量+加油包 ¥144/90 GiB」与「标准 ¥159/100 GiB」只差 ¥15 |
| 9 | **SLO / on-call** | 🔶 [ADR 0014](../05-adr/0014-slo-and-oncall.md)（**提案，未批准**）。⚠️ 即便批准，0014 §4.1 也明说 `DP-a` 在 P1 期**故意没有 SLO** |

> **新增第 10 条（本次审查提出）：`deploy.yml` 的四项 TODO 里有两项是裁决而不是配置** ——
> `BP_WEB_DEPLOY_CMD` 等 ADR 0003 的托管选型，staging 环境用不用独立 GCP 项目也没裁决过。
> 见 §7 的 B47。

---

## 7 · 三条新登记的阻塞项（B47–B49）

| # | 一句话 | 为什么现在才登记 |
|---|---|---|
| **B47** | **`deploy.yml` 从未运行过**（35 次 run 全是 `ci`），四项 TODO 未填，仓库 **0 个 environment / 0 个 variable / 0 个 secret** | 九天前 §7.5 只把它记成一条「工程卫生」。实查之后它更像一条结构问题：**写下来的部署路径与实际用的部署路径是两条不同的东西**，没有一条被 CI 验证过 |
| **B48** | **管理面与内部面鉴权未接线**：`authmap.go` 对 **61 个** `/admin/*` + **9 个** `/internal/tasks/*` 共 70 个 operation **硬性 501 fail-closed** | 这是**刻意的**（注释写明：上一版原样放行，任何人实现某个 admin handler 的那一刻就等于上线一个无鉴权管理端点，而 diff 里看不出异常）。🔶 **本轮正在接**：在 HEAD `a4604c9396f` 上，`AuthenticateAdmin` / `AuthenticateInternal` 已写在 `api/internal/middleware/`，但 `authmap.go` **还没接上，也没有对应测试**。<br>🔶 **2026-08-30 收尾时的现场更新（同一天内，本审查进行期间）**：另一条并行工作流已经在 **未提交的工作树**里把这条接线做完了 —— `authmap.go` 的 `case adminOperations[...]` 与 `case internalTaskOperations[...]` 已拆成两个独立分支，分别调 `mw.AuthenticateAdmin` 与 `mw.AuthenticateInternal`，并新增 5 个测试文件（`middleware/admin_test.go`、 `middleware/internal_test.go`、`cmd/server/authwiring_test.go`、`internal/audit/audit_test.go`、 `internal/config/admin_test.go`），Go 测试从 **20 文件 / 195 函数** 涨到 **25 文件 / 275 函数**。 拆分买下的正是那条注释担心的东西：**从此「实现某个 admin handler」不再等于「上线一个无鉴权端点」** （鉴权不过 → 403 进不了 handler；凭据对了但 handler 没写 → 仍然 501）， 且 `BP_ADMIN_IAP_AUDIENCE` 未配置时 `AuthenticateAdmin` **整体拒绝**（fail-closed）， 「配置漏了」的现象是「管理面进不去」而不是「谁都进得去」。 🔴 **但本条仍记 🔶 不记 ✅，两条理由**：① **这些改动尚未提交**， 本文的事实基线是 master `a4604c9396f`，按本仓口径 **未提交 ≠ 已落库**； ② **接线 ≠ 端点可用** —— 61 个 admin handler 绝大多数仍是 `Unimplemented` 的 501， `BP_ADMIN_IAP_AUDIENCE` 也还没有在生产 `bp-api` 上配置过。 |
| **B49** | **sqlc 检查不到写生成列**（`sqlc.yaml` 无 `database:` 段），已由本轮两步 CI 补上 | ✅ 已解决：`db_explain.py` 对 **194 条**生成 SQL 跑 `EXPLAIN (GENERIC_PLAN)`；`ci_post_rollback_write.sql` 在 `0016.down` 之后**先 INSERT 一行再打到那一行**并断言 `ROW_COUNT=1`。🔴 值得记一笔：ADR 0013 §6.4 原提议的 `UPDATE … WHERE false` **抓不到它要抓的东西**（影响 0 行 ⇒ 触发器不执行 ⇒ plpgsql 永不解析字段名） |

**B1–B49 总账**（详见 [roadmap §9](roadmap.md)）：**✅ 已解决 13 · 🔶 部分 18 · 🔴 开放 18。**

> 🔴 **B5 / B16 / B21 / B24 / B25 / B26 / B29 / B34 / B35 这九条一律记 🔶 而不是 ✅，
> 只有一个理由：它们对应的 ADR 0011–0015 全部是「提案，未批准」。**
> 把「写了一份裁决」记成「解决了一个阻塞项」，是这张表最容易被用错的方式 ——
> 因为它读起来像进度，实际只是**把同一个问题写得更清楚了**。

---

## 8 · 通往首笔收款的关键路径（本次修订）

硬性时间窗（不可压缩）：域名可达性采样 1 周 · 节点单人验证 72 小时 · 灰度 7 天。

| 序 | 动作 | 链 | 依赖 | 与九天前的差别 |
|---|---|---|---|---|
| 0 | 汇率用权威源核实一次（Fed H.10 / 人民银行中间价） | 商业 | 无 —— **今天一次查询** | **新增**。它是定价页上线硬前置，且**双侧**：核实值 < 7.00 就要下调全表 |
| 1 | **建 `bp-node-hk1`** —— 🔴 **不必再等域名** | 技术 | 无（`babel.plus` 已可签 LE / DNS-01） | **变了**：九天前这一步「卡在域名」，而那个前提是错的 |
| 2 | 装机 9 步 → **72 小时单人验证** = P1 出口 | 技术 | 1 | 不变 |
| 3 | 接通 ESP 发信 | 商业 | ⏳ 用户选 ESP | **升优先级**：它是唯一的失联恢复通道，且注册验证码现在发不出去 |
| 4 | `k` 对账（第一个完整账期）+ `nettier-ab-*` | 双链 | 1、2 + ⏳ 决策 6 | **新增**：`k` 是定价里唯一无出处又直接乘在变动成本上的参数 |
| 5 | 接线管理面鉴权（B48）→ 后台 17 个模块才可能接线 | 技术 | 无 | **新增**：九天前这条藏在「后台约 60 个 admin operation 全 501」里，没被识别为**鉴权**问题 |
| 6 | 支付网关落地 + 前端关键路径 8 页（现已接 2 页） | 商业 | ⏳ 决策 3、P1 出口 | 不变 |
| 7 | 3–5 人灰度 **7 天** | 汇合 | 2 + 6 | 不变 |
| 8 | **第一笔真实 USDT 收款「下单 → 到账 → 自动开通 → 对账」= 上线** | 汇合 | 全部 | 不变 |

> **本次最重要的路径变化只有一条**：第 1 步的前置从「⏳ 等用户注册域名」变成「**无**」。
> 技术链上那个被认为不在我们手上的起点，其实一直在我们手上。

---

## 9 · 与 [20260821 那份](launch-readiness-review-20260821.md) 的逐条对读

> 那份是 As-Built、不回改。本节只做对读，**不修改它的任何一个字**。

| 20260821 的说法 | 2026-08-30 的实况 | 性质 |
|---|---|---|
| §1「生产与 master 漂移」是头号发现 | ✅ **已消除**。PR #8–#18 全部合并，**0 个 open PR** | 真进展 |
| §1「ADR 0001 批准记录只在分支上」 | ✅ **已在 master**（`a70a9621298` 是 HEAD 的祖先，实查） | 真进展 |
| §1 / §2.2 / §3「域名 **0 个注册**」 | 🔴 **前提是错的。** `babel.plus` 已注册且 DNS 可控（`dig` 实查） | **纠错** |
| §2.1「API 18/128，155 顶层测试」 | 18/128 **不变**；测试 **155 → 195**（+40） | 真进展（测试） |
| §2.1「12 组迁移、41 张表、189 条 sqlc 查询」 | **17 组 / 44 张表 / 194 条查询** | 真进展 |
| §2.1「Web 双 SPA 空壳，无登录守卫，**测试 0**」 | **108 个用例全绿**，登录态 + 16 条守卫路由；**但仍只有 3 个路由组件调过 API** | 半进展 |
| §2.1「infra 11 个脚本 5,571 行」 | **18 支 10,705 行** | 工作量增加，**执行次数仍是 0** |
| §2.1「CI 8 作业全绿；`deploy.yml` 从未执行」 | **9 作业**；`deploy.yml` **仍是 0 次**，且仓库 0 environment/variable/secret | **恶化认知**（问题更清楚了，没改善） |
| §2.2「不存在任何 `bp-node-*` / `bp-web` / 已注册域名」 | `bp-node-*` **仍 0 台**、`bp-web` **仍未部署**；域名那一条**是错的** | 部分纠错 |
| §3「P0 ≈ 80%」 | **按出口标准 3.5/7 = 50%**（标准 6 是 0/3）；按工作量 ≈ 90% | **口径更严** |
| §3「P1 ≈ 40%」 | **按出口标准 0/8 = 0%**；按工作量 ≈ 45% | **口径更严** |
| §3「P2 ≈ 5%」 | 出口标准 0/8；工作量 ≈ 15%（定价定案 + 前端起步） | 小幅真进展 |
| §3「P3/P4 = 0%」 | **仍是 0%** | 不变 |
| §4「这笔账目前算不出来」 | ✅ **算得出来了**：三档定案、24 格覆盖 1.2749–1.5616×、满编 $412.24 vs 收入 $597.76 | **本期最实质的进展** |
| §5 第 1 条「合并 PR #8→#9→#10」 | ✅ 做完 | — |
| §5 第 3 条「SKU 级拆分」 | ✅ 做完（`egress-billing-20260820`） | — |
| §5 第 4 条「起真实 v2node 容器验 401/403」 | 🔶 **答案有了（读源码），容器仍未起** | 半 |
| §5 第 5 条「`openssl` 核对证书签发者」 | ✅ 做完，答案 GTS。**但「若是 GTS 则需两个入口」指向的代理选型仍未定** | 半 |
| §5 第 6 条「抓客户端 UA」 | 🔴 **仍未做**（B17，七条「成本以分钟计」里唯一零进展的那条） | 不变 |
| §5 第 7 条「修四处文档失同步」 | ✅ 全部修完，**且本轮又发现并修了 12 处以上**（§5） | 真进展 |
| §8「已解决 8 · 部分 4 · 开放 28」（B1–B40） | **B1–B49：已解决 13 · 部分 18 · 开放 18** | 条目数 +9，已解决 +5 |
| §11 代价 1「本文是时点快照，发布即开始过时」 | **应验**。§2/§3/§8 的数字九天内全部失准，这正是本文另写一份而不改它的原因 | — |
| §12「本文未修复它发现的任何问题」 | ✅ **本轮修了**：§5 那张表就是它 | — |

**九天的净变化，一句话：**
**可上线性零位移（节点 0 台、收款 0 笔、告警 0 条、部署 0 次），
但「我们究竟差什么」这个问题的答案从模糊变得可判定** ——
代价是完成度数字看起来**退步**了（P0 80% → 50%，P1 40% → 0%），
而那不是退步，**是换了一把更诚实的尺子**。

---

## 10 · 代价

> ⚠️ 这一选择的代价，必须留在记录里而不是被措辞掩盖：
>
> 1. **本文是时点快照，发布即开始过时。** 事实基线钉在 2026-08-30 的 master `a4604c9396f`。
>    建一台节点、批准一份 ADR、跑一次 `deploy.yml`，都会让 §2 / §3 / §7 的数字失准。
>    **本文不回改** —— 后续进展应更新 [roadmap.md](roadmap.md) 与各活文档，而不是维护这份快照。
>    （这条与 [20260821 §11 代价 1](launch-readiness-review-20260821.md) 同构，而那条**已经应验**：
>    它的 §2/§3/§8 在九天内全部失准。**同构的代价意味着同构的结局。**）
> 2. 🔴 **本次没有跑 `go test`，因为本机没有 Go 工具链**（`which go` 无结果）。
>    §2.1 的「195 个顶层 `Test` 函数」是 `grep` 的静态计数，**不是一次绿灯**。
>    **代价很具体**：如果这 195 个里有失败的，本文不知道，而本文的其它判断
>    （比如「API 侧 18 个 operation 可用」）部分依赖它们是绿的。
>    web 侧的 108 是真跑出来的，Go 侧不是 —— **两个数字的证据强度不一样，不要并列引用。**
> 3. 🔴 **本次没有跑 `gcloud`，§2.2 的 GCP 实况全部转述自 2026-08-20 的 as-built。**
>    也就是说「`bp-node-*` 0 台」「`bp-web` 未部署」「17 条告警一条没建」这三句
>    是**十天前的实况 + 期间无相关 commit** 推出来的，**不是本次实查**。
>    它们与其它数字的证据强度不同，已在 §2.2 标明口径。
> 4. **把完成度按出口标准算，代价是数字看起来比九天前退步。**
>    P0 从 80% 到 50%、P1 从 40% 到 0% —— 而这九天实际是**有产出**的。
>    一个「按更严口径算出来的退步」在压力下极易被改回宽口径，
>    而 [roadmap §12 代价 6](roadmap.md) 已经预言过这件事：
>    「在只有出口标准没有回退规则的排期里，压力下的默认行为是**降低出口标准**」。
>    **本文能提供的唯一防护是把两个数字并列写出来**，让「换尺子」这个动作可见。
> 5. **§9 的逐条对读把两份审查压成一张表，丢失了各自的语境**
>    （与 [20260821 §11 代价 2](launch-readiness-review-20260821.md) 同构）。
>    表里每行都指了出处，但读表的人不会去翻。
>    **已知的具体损失**：20260821 §3 那句「P1 ≈ 40%」在它的原文里是有解释的
>    （「API 侧 18 op 上云；节点侧为零」），到了 §9 的表里被压成「40% → 0%」两个数字，
>    看起来像项目倒退。
> 6. **本文用了 roadmap 自己的出口标准做尺子，而没有质疑那套标准。**
>    已知一处张力：P1 出口标准 4（「订阅链接在两个客户端各人工加载一次成功」）
>    是**不可自动化**的判据，而 B45 / B46 两条都指向它会失败 ——
>    也就是说 P1 可能存在一个「其余七条都过、第 4 条过不了」的状态，
>    而 roadmap **没有为任何阶段定义「失败」**（§12 代价 6 自陈）。本文同样没有。

## 11 · 这次没有解决的

- [ ] 🔴 **没有跑 `go test ./...`**（本机无 Go 工具链）。不在本次范围是因为装工具链会改动环境，
      而本次是文档审查。**下一次审查前应当先解决这一条** —— 195 这个数字的证据强度
      比 web 侧的 108 低一整档，而本文多处判断依赖 Go 侧测试是绿的。
- [ ] 🔴 **没有跑 `gcloud` 复核 GCP 实况**（§2.2 全部转述自 2026-08-20 的 as-built）。
      「`bp-node-*` 0 台」 这条判定是本文的承重墙之一（P1 出口 0/8 全靠它），
      而它**不是本次实查的**。不在本次范围是因为本次审查只覆盖仓库侧。
- [ ] **没有验证生产 `bp-api` 当前 serving revision 与 master HEAD 的对应关系。**
      与 [20260821 §12](launch-readiness-review-20260821.md) 第 2 条同一条欠账，**九天里没有还**。
      `infra/scripts/image-provenance.sh`（PR #16）现在提供了这个能力（一条命令查出完整 sha
      并判断该 commit 是否仍被分支引用），**但本次没有执行它**。
- [ ] **没有做代码审查。** 本文只核对「实现了什么、测了什么、跑没跑过」，
      没有逐行看 `2c0c6b69bde` 与 `a4604c9396f` 这两个大 commit 的正确性。
      ⚠️ 这一点值得警惕：`a4604c9396f` 的提交信息本身就写着上一个提交**把主干弄坏了**
      （生成列破坏付款链），而那个错误是**在写完之后才被发现的**。
- [ ] **`user-journey.md` 的价格占位没有填。** pricing 的价格已定案，
      而 user-journey §16 仍写「所有价格数字留空 —— 依赖 pricing §7 第一条」。
      本次只在该条上登记了「被依赖的那一半已经不成立」，**没有填数字** ——
      填数字要连带核对旅程各步的金额示例与文案，属独立一次修订。
- [ ] **P1 出口标准 4 的「可能过不了」没有对应的阶段门。**
      见 §10 代价 6。roadmap 没有为任何阶段定义「失败」，本文没有补上这个空缺。
- [ ] **`web/README.md` 的章节顺序违反 [docs/README §4.1](../README.md)，只登记未修。**
      它是 `§7 代价 → §8 这次没有解决的 → §9 测试`，而规矩要求前两者物理最后。
      不修是因为改序要重编号，而 §7/§8/§9 在本文与 [roadmap](roadmap.md) 里都被按号引用 ——
      **与 pricing 那次「不重编号」是同一个取舍**。已在 `web/README.md §8` 就地登记。
- [ ] **没有评估「内部用户手工收款」这条捷径。**
      与 [20260821 §12](launch-readiness-review-20260821.md) 第 4 条同一条欠账。
      若内部用户少到可以手工收款 + D6 手工标记，§8 的第 6 步可以大幅降级、路径显著缩短 ——
      但那要求先回答决策 3，本文不代替该裁决。
